# Request: 2026-02-18__state-transition-validation

## 要望の原文

- `state.json` の状態遷移整合を自動検証する高優先タスクを具体化する。

## 要望の整理

- task 状態の必須値と遷移ルールを検証する validator を追加する。
- `done` 判定時の最低条件（例: review 完了）を検査する。
- CI で実行して不正状態を早期検知する。

## 成功条件（要望レベル）

1. 不正 state 値を検出できる。
2. 不整合遷移を検出できる。
3. CI で自動実行される。
